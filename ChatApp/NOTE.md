# Claude.md Files /init

save memory /memory
https://code.claude.com/docs/en/memory

# Context
using anotation @

too much uncessary context can lead to less accurate results

use the /exit command
exits the chat session completely

use the /clear command
clears the entire session context and chat history

use the /compact command
compacts the chat & context into  a small summary

press esc twice
rewind to a previous point in the session

use  the /exit command
use the /resume command

# Tools and permissions
https://code.claude.com/docs/en/tools-reference
https://code.claude.com/docs/en/permissions