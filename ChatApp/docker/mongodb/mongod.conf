# MongoDB config for 100k user scenario
storage:
  dbPath: /data/db
  journal:
    enabled: true
  wiredTiger:
    engineConfig:
      cacheSizeGB: 0.5

net:
  port: 27017
  bindIp: 0.0.0.0

operationProfiling:
  slowOpThresholdMs: 200
  mode: slowOp
